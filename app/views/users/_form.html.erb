<%= form_for @user do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    First Name <%= f.text_field :first_name, placeholder: "First" %><br>
    Last Name <%= f.text_field :last_name, placeholder: "Last" %><br>
    Email <%= f.email_field :email, placeholder: "Email" %><br>
    Password <%= f.password_field :password, placeholder: "Password" %><br>
    Password Confirmation <%= f.password_field :password_confirmation, placeholder: "Password Confirmation" %><br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
